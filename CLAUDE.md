# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Purpose

This is a GitHub profile repository (`l3titb3/l3titb3`). The `README.md` at the root is the **only meaningful file** — GitHub renders it directly on the user's public profile page at github.com/l3titb3.

There is no build system, test suite, or application code. All work here is editing the profile README.

## README.md Format

The profile README supports standard GitHub Flavored Markdown. Changes are visible immediately after pushing to the `main` branch — no build or deployment step required.
